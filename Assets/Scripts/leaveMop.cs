using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class leaveMop : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        itemTracker.hasItem3 = false;
    }

    // Update is called once per frame
    void Update()
    {

    }
}
